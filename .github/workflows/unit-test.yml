name: build

on:
  push:
    branches: [ "main" ]

jobs:
  run-build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - run: cd ./code/
    - run: echo MNEMONIC="" > .env
    - run: make install
    - run: make build
    - run: make test